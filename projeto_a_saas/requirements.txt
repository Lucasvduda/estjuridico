# LegalShield AI 2026 — Projeto A (SaaS)
# Dependências do sistema multi-tenant

# === Framework ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# === Banco de Dados ===
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.1

# === Autenticação e Segurança ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyotp==2.9.0
qrcode[pil]==8.0

# === Validação ===
pydantic[email]==2.10.4
pydantic-settings==2.7.1

# === Criptografia ===
cryptography==44.0.0

# === LLM ===
litellm==1.55.8
openai==1.59.5
anthropic==0.42.0

# === Processamento de Documentos ===
PyMuPDF==1.25.3
python-docx==1.1.2
pytesseract==0.3.13
Pillow==11.1.0
chardet==5.2.0

# === Relatórios PDF ===
reportlab==4.2.5

# === Logging ===
structlog==24.4.0

# === Rate Limiting ===
slowapi==0.1.9

# === Cache + Fila Assincrona ===
redis[hiredis]==5.2.1
arq==0.26.1

# === Object Storage (Cloudflare R2 / S3-compatível) ===
# Usado quando STORAGE_BACKEND=r2 (produção 10k+ usuários).
# Em dev local com STORAGE_BACKEND=redis ou memory, este pacote é importado
# mas nunca chamado — não é necessário configurar credenciais R2.
boto3==1.37.38

# === Utilitários ===
python-dotenv==1.0.1
httpx>=0.23.0
jinja2==3.1.4
